#include "autons.hpp"
#include "subsystems/subsystems.hpp"

void autons::capSideDefault(Side side, bool park) {
    bool angle_mult = side == side_blue ? -1.f : 1.f;

    // launch ball at middle flag
    
}
